import argparse
import cv2
impport random
from random import randrange

Ordner =['r'C:\Programm Files', 'r'C:\Desktop']

Home = 'r\C:Programm Files\Home'
if not os.path.exists(Home)
os.makedirs(Home)
if os.path.exists(Home)
shutil.move(Haustier.py, 'r\C:Programm Files\Home, copy_function=copy2)

if event == cv2.EVENT_LBUTTONDOWN:
  shutil.move(Haustier.py,(random_index = randrange(len(Ordner)), copy_function=copy2)
  item = Ordner[random_index]
